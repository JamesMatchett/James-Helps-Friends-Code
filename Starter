from urllib.request import urlopen
with urlopen('testsite') as response:
	for line in response:
	         line = line.decode('utf-8')
	         if 'Air' in line or 'Rhine' in line:  # look for temperature
	             print(line)
